{
  "pluginAlias": "GivEnergy Local + Intelligent Octopus Go",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "GivHome brings Intelligent Octopus Go smart charging to your GivEnergy battery \u2014 automatically, locally, and beautifully inside Apple Home.",
  "schema": {
    "type": "object",
    "properties": {
      "inverterSerial": {
        "title": "Battery Serial Number",
        "type": "string",
        "default": "",
        "description": "Required. Find this in the GivEnergy app: Settings \u2192 Local Monitoring \u2192 Scan for your inverter. Example: CH2344G083.",
        "minLength": 6,
        "maxLength": 20,
        "pattern": "^([A-Z]{2}\\d{4}[A-Z]\\d{3}|[A-Z0-9]{6,20})$"
      },
      "maxPvKw": {
        "title": "Maximum Solar Power (kW)",
        "type": "number",
        "default": 0,
        "minimum": 0,
        "maximum": 100,
        "description": "Optional. Maximum PV/solar output used to scale the Solar tile. Leave blank or set to 0 if this battery system does not have solar PV. Example: 2.88 for a 2.88 kW array."
      },
      "octopusAccountNumber": {
        "title": "Octopus Account Number",
        "type": "string",
        "default": "",
        "description": "For Intelligent Octopus Go smart dispatch windows. Usually starts with A-. Leave blank only if you want monitoring/manual control/off-peak scheduling without Octopus dispatch."
      },
      "octopusApiKey": {
        "title": "Octopus API Key",
        "type": "string",
        "default": "",
        "description": "Find this in your Octopus Energy online account under Developer Settings / API access. Used only to read Intelligent Octopus Go dispatch windows; leave blank only if not using IOG dispatch."
      },
      "targetSoc": {
        "title": "Target Charge %",
        "type": "integer",
        "default": 100,
        "minimum": 1,
        "maximum": 100,
        "description": "Default battery target for automatic and manual charge slots. Leave at 100 unless you intentionally want a lower target."
      },
      "cheapStart": {
        "title": "Off-Peak Start",
        "type": "string",
        "default": "23:30",
        "description": "Default Intelligent Octopus Go off-peak start time. Leave at 23:30 unless your tariff differs. Use HH:MM 24-hour format, for example 23:30.",
        "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
      },
      "cheapEnd": {
        "title": "Off-Peak End",
        "type": "string",
        "default": "05:30",
        "description": "Default Intelligent Octopus Go off-peak end time. Leave at 05:30 unless your tariff differs. Use HH:MM 24-hour format, for example 23:30.",
        "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$"
      },
      "graceMinutes": {
        "title": "Grace Period",
        "type": "integer",
        "default": 30,
        "minimum": 0,
        "maximum": 30,
        "description": "After an Octopus smart dispatch ends, continue only until the end of the current half-hour period (:00 or :30). Leave at 30 for this behaviour."
      },
      "name": {
        "title": "Homebridge Platform Name",
        "type": "string",
        "default": "GivHome",
        "description": "Advanced. Internal Homebridge platform display name. Leave as GivHome."
      },
      "mqttUrl": {
        "title": "MQTT URL",
        "type": "string",
        "default": "mqtt://127.0.0.1:1884",
        "description": "Advanced. Local MQTT broker used by GivTCP telemetry. Leave unchanged for the standard GivHome appliance where host port 1884 maps to Mosquitto container port 1883. Existing non-appliance installs may use their own broker."
      },
      "mqttUsername": {
        "title": "MQTT Username",
        "type": "string",
        "default": "",
        "description": "Advanced. Only needed if your MQTT broker requires authentication."
      },
      "mqttPassword": {
        "title": "MQTT Password",
        "type": "string",
        "default": "",
        "description": "Advanced. Only needed if your MQTT broker requires authentication."
      },
      "mqttRootTopic": {
        "title": "MQTT Root Topic",
        "type": "string",
        "default": "GivEnergy",
        "description": "Advanced. Root MQTT topic published by GivTCP."
      },
      "givTcpRestUrl": {
        "title": "GivTCP Local Address",
        "type": "string",
        "default": "http://127.0.0.1:6345",
        "description": "Advanced. Local GivTCP REST control endpoint. Leave unchanged unless GivTCP is running on another device. If you need to locate your inverter, use the GivEnergy app: Settings \u2192 Local Monitoring \u2192 Scan for your inverter."
      },
      "octopusPollSeconds": {
        "title": "Octopus Polling Seconds",
        "type": "integer",
        "default": 120,
        "minimum": 30,
        "maximum": 1800,
        "description": "Advanced. How often the plugin checks Octopus for updated smart charging windows."
      },
      "slotToleranceMinutes": {
        "title": "Manual Tile Tolerance",
        "type": "integer",
        "default": 5,
        "minimum": 0,
        "maximum": 10,
        "description": "Extra time allowed when checking whether manual charge or export slots are still active. Helps absorb inverter clock drift and telemetry delays."
      },
      "powerActiveThreshold": {
        "title": "Legacy Shared Power Active Threshold W",
        "type": "integer",
        "default": 20,
        "minimum": 0,
        "maximum": 5000,
        "description": "Legacy fallback only. If the new separate thresholds are unset, this value is used as their default. New installs should use the individual threshold controls below."
      },
      "staleSeconds": {
        "title": "Telemetry Stale Seconds",
        "type": "integer",
        "default": 180,
        "minimum": 30,
        "maximum": 3600,
        "description": "How long MQTT telemetry can be silent before the inverter is shown as offline."
      },
      "smartWindowsJson": {
        "title": "Manual Smart Windows JSON",
        "type": "string",
        "default": "[]",
        "description": "Advanced fallback only. Optional extra cheap windows, e.g. [{\"start\":\"13:30\",\"end\":\"15:00\"}]."
      },
      "inverterIp": {
        "title": "Inverter IP Address",
        "type": "string",
        "default": "",
        "pattern": "^(25[0-5]|2[0-4]\\d|1?\\d?\\d)(\\.(25[0-5]|2[0-4]\\d|1?\\d?\\d)){3}$",
        "description": "Required for new appliance setup. Find this in the same GivEnergy app scan result as the Battery Serial Number: Settings \u2192 Local Monitoring \u2192 Scan for your inverter. Example: 192.168.0.223."
      },
      "chargePowerActiveThreshold": {
        "title": "Battery Charging Threshold (Watts)",
        "type": "integer",
        "default": 100,
        "minimum": 0,
        "maximum": 5000,
        "description": "Wattage threshold used to decide when the Battery Charging Light in Apple Home becomes active. Increase this value to ignore smaller or less interesting power changes. Set to 0 to disable this observation."
      },
      "dischargePowerActiveThreshold": {
        "title": "Battery Discharging Threshold (Watts)",
        "type": "integer",
        "default": 100,
        "minimum": 0,
        "maximum": 5000,
        "description": "Wattage threshold used to decide when the Battery Discharging Light in Apple Home becomes active. Increase this value to ignore smaller or less interesting power changes. Set to 0 to disable this observation."
      },
      "importPowerActiveThreshold": {
        "title": "Grid Import Threshold (Watts)",
        "type": "integer",
        "default": 50,
        "minimum": 0,
        "maximum": 5000,
        "description": "Wattage threshold used to decide when the Grid Import Light in Apple Home becomes active. Increase this value to ignore smaller or less interesting power changes. Set to 0 to disable this observation."
      },
      "exportPowerActiveThreshold": {
        "title": "Grid Export Threshold (Watts)",
        "type": "integer",
        "default": 50,
        "minimum": 0,
        "maximum": 5000,
        "description": "Wattage threshold used to decide when the Grid Export Light in Apple Home becomes active. Increase this value to ignore smaller or less interesting power changes. Set to 0 to disable this observation."
      },
      "exposeAdvancedTelemetry": {
        "title": "Extra Indicators in Apple Home",
        "type": "boolean",
        "default": false,
        "description": "Optional. Adds extra informational Lights to Apple Home for states such as Cheap Rate, Grace Period, Battery Charging, Battery Discharging, Grid Import, Grid Export, and Online. These are indicators, not controls."
      },
      "enableEveEnergyHistory": {
        "title": "Energy History in the Eve App",
        "type": "boolean",
        "default": false,
        "description": "Optional beta. Creates Eve Solar History, Eve Import History, and Eve Export History accessories so the Eve app can show local energy graphs and trends. These history tiles are informational only; in Apple Home, we recommend setting them to Exclude from Home View."
      }
    },
    "required": [
      "inverterSerial",
      "inverterIp"
    ]
  },
  "form": [
    {
      "type": "fieldset",
      "title": "Required Setup",
      "description": "Most users only need these details. The Battery Serial Number and Inverter IP Address are both shown in the GivEnergy app under Settings \u2192 Local Monitoring \u2192 Scan for your inverter.",
      "items": [
        "inverterSerial",
        "inverterIp",
        "maxPvKw",
        "octopusAccountNumber",
        "octopusApiKey"
      ]
    },
    {
      "type": "fieldset",
      "title": "Charging Behaviour",
      "expandable": true,
      "expanded": false,
      "description": "Defaults are set for Intelligent Octopus Go. Open only if you want to change target charge, off-peak times, or grace behaviour.",
      "items": [
        "targetSoc",
        "cheapStart",
        "cheapEnd",
        "graceMinutes"
      ]
    },
    {
      "type": "fieldset",
      "title": "Personal Preference Settings",
      "expandable": true,
      "expanded": false,
      "description": "Optional settings for extra Apple Home tiles, Eve energy history, manual tile tolerance, and observation thresholds. Smart Window shows as \u2018On\u2019 when Intelligent Octopus smart charging is active. For cleaner Apple Home use, set non-daily GivHome tiles to Exclude from Home View.",
      "items": [
        "exposeAdvancedTelemetry",
        "enableEveEnergyHistory",
        "slotToleranceMinutes",
        "chargePowerActiveThreshold",
        "dischargePowerActiveThreshold",
        "importPowerActiveThreshold",
        "exportPowerActiveThreshold"
      ]
    },
    {
      "type": "fieldset",
      "title": "Advanced Local Connection",
      "expandable": true,
      "expanded": false,
      "description": "MQTT, GivTCP, polling, and telemetry timeout settings. Leave unchanged for the standard local setup.",
      "items": [
        "mqttUrl",
        "mqttUsername",
        "mqttPassword",
        "mqttRootTopic",
        "givTcpRestUrl",
        "octopusPollSeconds",
        "staleSeconds",
        "name"
      ]
    }
  ]
}
